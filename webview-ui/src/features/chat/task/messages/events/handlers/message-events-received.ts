/**
 * Register messages event handlers on the given EventBridge.
 *
 * These handlers process message-list-related events sent from the backend.
 * Currently a placeholder — handlers can be added as needed.
 */
export function registerMessageEvents(_provider: Record<string, unknown>): void {
	// Future: register EventBridge listeners for MessagesEventKeys
}
