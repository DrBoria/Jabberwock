import type { IntentBus } from "../../../../intents/bus"

/**
 * Register all frontend window-manager event handlers on the IntentBus.
 */
export function registerOnFrontendWindowManagerIntents(_bus: IntentBus): void {
	// Window-manager handlers to be registered when created
}
