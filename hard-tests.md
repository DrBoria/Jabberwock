Первый тест

Ты требуешь создать у менеджера задачу создать функцию на C++ которая напишет hellow world скрипт на python и обязательно делегировать эту задачу designer, разбить на 6 шагов
После получения todo ты заменяешь designer в todo на backend developrt, меняешь 2-3 пункта местами, обазятельно 1 пункт передвигаегь куда-то или вообще удаляешь и проверяешь, в каком порядке он делает поставленные тобой задачи.
А главное ты тестируешь, что таски были рапределены, корректно были вынесены в chil process, затем только summary встроено в основной поток и что manager генерирует summary на базе summary каждой из задач, а не только своей логики

Дальше тест 2

Ты ставишь много задач - вывести в консоль приветствие, написать hello world на js, написать тест на hello world, вывести в консоль текущую дату и тд - такие простые, гранулярные задачи, которые можно делать независимо, делаешь их 10 штук
Разделяешь на разных агентов, которые есть в todo (главное, чтобы все одному агенту не достались, а минимум 3м агентам)
После чего все кроме одной ставишь в async режим и проверяешь, как работают асинхронные задачи, именно - правда ли они асинхронно выполняются, или всё-таки синхронно.
Понятно, что ollama у меня одна, но во первых, есть batching queries, а во вторых асинхронность не есть паралельно.
Так что проверяй асинхронность и далее - правильное вхождение чатов в задачу.
А главное ты тестируешь, что таски были рапределены, корректно были вынесены в chil process, затем только summary встроено в основной поток и что manager генерирует summary на базе summary каждой из задач, а не только своей логики
обязатаельно выйди из child task в parent, проверь, что есть пузырёк агента, а основное окно всё ещё корректно отображается.
Затем вернись в child task и проверь, что контент продолжает генерироваться

Дальше тест 3

Мы тестируем как процессы запускаются, воздают треды и возвращаются назад.
Т.е. ты говоришь агенту, например - создать html с красивой вёрсткой чегонибудь (сам придумай), затем нужно эту вёрстку адаптировать к мобилкам (т.е. уже будет явно открытие браузера, вызов функции designer которая проверит чё там как с внешним видом через VL модель@contextScopeItemMention - эта функция) и тд.
И потом ты делаешь 2-3 скриншота и удаляешь лишние файлы html. Т.е. в конце теста остаются только скриншоты.
Тут основное тестирование на таски, на вызов разных тулов, нужно протестировать, что тулы вызваны, отработали корректно, скриншоты создались, так же параллельно проверяешь, что html файлы были, потом они были изменены для мобилки, что они корректно удалены.
А главное ты тестируешь, что таски были рапределены, корректно были вынесены в chil process, затем только summary встроено в основной поток и что manager генерирует summary на базе summary каждой из задач, а не только своей логики
обязатаельно выйди из child task в parent, проверь, что есть пузырёк агента, а основное окно всё ещё корректно отображается.
Затем вернись в child task и проверь, что контент продолжает генерироваться
