/**
 * Foundation feature — no standalone event handlers.
 * Foundation handlers are registered by sub-features (window-manager, mst, time-machine).
 */
export function registerFoundationEvents(): void {
	// Sub-features register their own handlers
}
