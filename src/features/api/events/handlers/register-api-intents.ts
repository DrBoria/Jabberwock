import type { IntentBus } from "@features/intents/bus"

/**
 * Register API event handlers on the IntentBus.
 * API events are received from the frontend and dispatched via IntentBus.
 */
export function registerOnApiIntents(_bus: IntentBus): void {
	// Placeholder for frontend-to-backend API event handlers
}
